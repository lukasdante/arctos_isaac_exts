So like what I would do is to set up an OmniGraph that publishes a value right?
Then I adjust the value of that certain node to the get_value of the StringField? hope it works
But like how often does the new data gets ticked? it should be PER change only of the OmniGraph value